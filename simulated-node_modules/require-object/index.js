'use strict';

const path = require('path');

const projectRoot = require(path.join(__dirname, 'lib', 'project-root'));

module.exports = require(path.join(__dirname, 'lib', 'directory-structure-to-object'))(projectRoot);
