'use strict';

const path = require('path');


const projectRoot = require(path.join(__dirname, 'libs', 'project-root'));

module.exports = require(path.join(__dirname, 'libs', 'directory-structure-to-object'))(projectRoot);
